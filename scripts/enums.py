# Setup Python ------------------------------------------------------------------------------------------------------------------------- Setup Python #
from enum import Enum

class EntityType(Enum):
    PERSON = 1